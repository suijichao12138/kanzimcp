# Optional alternative to login_copilot.cmd; uncomment only when config.yaml
# uses github_token: ${GITHUB_TOKEN} and use_logged_in_user: false.
# GITHUB_TOKEN=ghp_replace_me

# BYOK examples (only needed when config.yaml selects that provider).
OPENAI_API_KEY=replace_me
AZURE_OPENAI_API_KEY=replace_me
ANTHROPIC_API_KEY=replace_me
LOCAL_MCP_TOKEN=replace_me
