<UserControl x:Class="KzNLPChatPlugin.KzNLPChatWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KzNLPChatPlugin"
             mc:Ignorable="d" 
             d:DesignHeight="480" d:DesignWidth="480">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 地址栏 + 连接按钮 -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="中继：" VerticalAlignment="Center" 
                       FontSize="12" Foreground="#888" Margin="0,0,4,0"/>
            <TextBox Grid.Column="1" x:Name="UrlBox" 
                     Text="ws://123.57.85.81:58080/用户名"
                     FontSize="11" VerticalAlignment="Center"/>
            <Button Grid.Column="2" x:Name="ConnectButton" Content="连接" 
                    Width="48" Height="24" FontSize="11" Margin="4,0,2,0"
                    Click="ConnectButton_Click"/>
            <Button Grid.Column="3" x:Name="DisconnectButton" Content="断开" 
                    Width="48" Height="24" FontSize="11" 
                    IsEnabled="False" Click="DisconnectButton_Click"/>
        </Grid>

        <!-- 聊天消息列表 -->
        <ScrollViewer Grid.Row="1" x:Name="MessageScroller"
                      VerticalScrollBarVisibility="Auto"
                      Background="#1E1E1E" BorderBrush="#555" BorderThickness="1"
                      Margin="0,0,0,6" Padding="8">
            <StackPanel x:Name="MessagePanel" />
        </ScrollViewer>

        <!-- 输入框 + 发送按钮 -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" x:Name="InputBox"
                     Height="28" FontSize="13"
                     Padding="4,3"
                     AcceptsReturn="False"
                     IsEnabled="False"
                     KeyDown="InputBox_KeyDown"/>
            <Button Grid.Column="1" x:Name="SendButton" Content="发送" 
                    Width="56" Height="28" Margin="6,0,0,0"
                    IsEnabled="False" Click="SendButton_Click"/>
        </Grid>
    </Grid>
</UserControl>
