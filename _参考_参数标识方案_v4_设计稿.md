# kz_invoke 参数类型盘点 + 标识设计方案

> 版本：v4 参数标识版（方案设计稿，未实现）
> 基础：v3_merged 的 KzMCPReflectionBridge.cs
> 日期：2026-08-04
> 状态：待老隋确认后实现

---

## 一、背景与目标

当前反射桥 `ResolveSingleArg` 靠**"自动猜测"**解析参数类型，顺序敏感、有歧义、不够精准：
- 字符串 `"123"` 默认转 int，但有时就是想传字符串 "123"
- 字符串 `"true"` 默认转 bool
- 节点路径 `/xxx` 默认转节点对象
- 只有 `@obj`（对象引用）和 `@type:`（新加）有显式标识

**目标**：给所有参数类型加**显式标识前缀**，做到：
1. **有标识 → 必须走标识**（精准、无歧义）
2. **无标识 → 保留自动猜测兜底**（兼容所有老调用）
3. 反射方法查找、泛型初始化更精准（不用猜、不用兜底 try）

---

## 二、当前参数的完整盘点（从代码 ReadResolveSingleArg + 泛型分支）

### 2.1 `ResolveSingleArg` 当前识别逻辑（v3_merged）

| 输入形式 | 解析为 | 触发条件 | 代码位置 |
|---------|--------|---------|---------|
| `"123"` | `int` | `int.TryParse` 成功 | 第 796 行 |
| `"true"`/`"false"` | `bool` | `bool.TryParse` 成功 | 第 797 行 |
| `"1.5"` | `float` | `float.TryParse` 成功 | 第 798 行 |
| `"@type:xxx"` | `Type` 对象 | `@type:` 前缀 | 第 796 行（新增） |
| `"@objXX"` | 对象引用 | `@` + `_objectStore.ContainsKey` | 第 806 行 |
| `"/节点路径"` | 节点对象 | 含 `/` + 非`@`非`{` + `GetNodeByPath` | 第 815 行 |
| `{...}` 字典 | `Dictionary<string,int>` | `IDictionary` | 第 822 行 |
| 普通字符串 | `string` | 以上都不满足，兜底 | 第 819 行 |
| JSON 数字/布尔（非字符串） | 原生 int/bool/float | 直接非字符串 | 隐式 |

### 2.2 enum 处理（不在 ResolveSingleArg，在 FindEnumMethod）

- `FindEnumMethod` 在**方法匹配阶段**把 int/string 参数按目标方法签名**自动转枚举**
- 转换依据：`paramType.IsEnum`
  - int → `Enum.ToObject(paramType, intVal)`
  - string → `Enum.Parse(paramType, strVal)`
- 位置：第 697 行起，`InvokeCore` 里先于 `FindMethod` 调用

### 2.3 泛型分支的 args[0] 处理（447-560 行）

- args[0] 是 Type 对象 → 直接用（新增 @type: 通道）
- args[0] 是字符串且 `@obj` 指向 RuntimeType → 取短类型名
- args[0] 是普通字符串 → 当类型名，`FindProjectItemType` 解析
- 泛型参数约定：`genericParamCount == totalArgCount - 1`（实参 = 泛型参数 + 1，args[0] 是 T）

### 2.4 其他解析入口（非 ResolveSingleArg）

| 入口 | 位置 | 说明 |
|------|------|------|
| `CallNodeMethod` | 第 684 行 | 便捷方法内部，`args.Select(a => a?.GetType())` 直接用类型 |
| `FindEnumMethod` | 第 697 行 | enum 自动转换 |
| `ConvertValue` | 第 2096 行 | 值转 string/int/bool/float |
| `CreateNode(parent, nodeType, name)` | 第 1033 行 | nodeType 裸字符串查库 |

---

## 三、标识设计方案

### 3.1 完整类型标识表

| 类型 | 标识前缀 | 示例 | 解析为 |
|------|---------|------|--------|
| int | `@int:` | `@int:123` | `int 123` |
| long | `@long:` | `@long:1234567890123` | `long` |
| float | `@float:` | `@float:1.5` | `float 1.5` |
| double | `@double:` | `@double:3.14` | `double` |
| decimal | `@decimal:` | `@decimal:99.9` | `decimal` |
| bool | `@bool:` | `@bool:true` | `bool true` |
| string | `@string:` | `@string:123` | `string "123"`（强制字符串） |
| char | `@char:` | `@char:a` | `char` |
| enum | `@enum:` | `@enum:WHOLE_PROPERTY` | 枚举（按方法签名精确定位） |
| Type | `@type:` | `@type:string` | `Type typeof(string)` |
| 对象引用 | `@obj:` | `@obj:10` / `@obj10` | `_objectStore` 对象 |
| 节点 | `@node:` | `@node:/Screens/.../x` | 节点对象（GetNodeByPath） |
| 字典 | `@dict:` | `@dict:{"a":1}` | `Dictionary<string,int>` |
| 字节数组 | `@bytes:` | `@bytes:base64...` | `byte[]`（可选） |

### 3.2 解析规则（核心）

```
if 参数是字符串:
    if 以 "@int:" 开头   → 解析 int（有标识，必须按标识）
    if 以 "@long:" 开头  → 解析 long
    if 以 "@float:" 开头 → 解析 float
    if 以 "@double:" 开头→ 解析 double
    if 以 "@decimal:" 开头→ 解析 decimal
    if 以 "@bool:" 开头  → 解析 bool
    if 以 "@string:" 开头→ 强制 string（去掉前缀）
    if 以 "@char:" 开头  → 解析 char
    if 以 "@enum:" 开头  → 标记为枚举名（交给 FindEnumMethod 精确定位）
    if 以 "@type:" 开头  → 解析 Type
    if 以 "@obj:" 开头   → 对象引用（拆出 obj号，查 _objectStore）
    if 以 "@node:" 开头  → 节点对象
    if 以 "@dict:" 开头  → 解析字典
    # —— 以上是"有标识"分支，明确指定 ——
    else（无标识）：
        # —— 以下保留"自动猜测"兜底 ——
        @objXX（老格式）→ 对象引用
        int.TryParse → int
        bool.TryParse → bool
        float.TryParse → float
        含 "/" 且非 @ 非 { → 节点
        { 开头 → 绑定表达式字符串
        其他 → string
```

### 3.3 关键设计点（有标识必须按标识，无标识保留猜测）

```csharp
private object ResolveSingleArg(object a)
{
    if (a is string s)
    {
        // ===== 显式标识分支（优先，有标识必须按标识）=====
        if (s.StartsWith("@int:"))    { return int.TryParse(s.Substring(5), out var v) ? (object)v : s; }
        if (s.StartsWith("@long:"))   { return long.TryParse(s.Substring(6), out var v) ? (object)v : s; }
        if (s.StartsWith("@float:"))  { return float.TryParse(s.Substring(7), out var v) ? (object)v : s; }
        if (s.StartsWith("@double:")) { return double.TryParse(s.Substring(8), out var v) ? (object)v : s; }
        if (s.StartsWith("@decimal:")){ return decimal.TryParse(s.Substring(9), out var v) ? (object)v : s; }
        if (s.StartsWith("@bool:"))   { return bool.TryParse(s.Substring(6), out var v) ? (object)v : s; }
        if (s.StartsWith("@string:")) { return s.Substring(8); }               // 强制 string
        if (s.StartsWith("@char:"))   { return s.Substring(6).Length > 0 ? (object)s.Substring(6)[0] : s; }
        if (s.StartsWith("@enum:"))   { return s; }  // 标记，交由 FindEnumMethod 按签名定位
        if (s.StartsWith("@type:"))   { /* 现有 @type 逻辑 */ }
        if (s.StartsWith("@obj:"))    { return ResolveObjReference("@obj" + s.Substring(5)); }
        if (s.StartsWith("@node:"))   { try { return GetNodeByPath(s.Substring(6)); } catch { return s; } }
        if (s.StartsWith("@dict:"))   { /* 解析字典 */ }

        // ===== 无标识 → 自动猜测兜底（兼容老调用）=====
        // 现有逻辑原样保留...
    }
}
```

### 3.4 enum 的精准匹配改进

现在 `@enum:XXX` 标识参数，在 `FindEnumMethod` 里**优先按标识精确匹配**：
- 遇到 `@enum:` 开头的参数，只尝试该符号名对应的枚举值
- 避免"智能猜测"可能选错重载（比如多个方法同名但枚举不同）

### 3.5 泛型初始化的精准

- `@type:` 已支持，args[0]=Type 直接用
- 现在多了 `@obj:`（对象引用）可作泛型 parent、`@enum:` 可作类型枚举实参
- 泛型分支的 `genericParamCount == totalArgCount - 1` 判断不变，但类型解析更精

---

## 三补、插件 API 实测盘点 — 新增/需补充的类型标识

> 来源：完整读了官方 plugin API reference（3.9.9），逐个接口摘出方法签名里的参数类型。
> 结论：**原方案漏了几个关键类型，必须补充**。

### 3.6 从 API 签名发现的新类型（原方案没覆盖）

| 类型 | API 出处（方法签名） | 说明 | 建议标识 | 必须？ |
|------|--------------------|------|---------|--------|
| **`null`（可空）** | `CreateFloatProperty(name, displayName, category, float? lowerBound, float? upperBound, float? step)` | 文档明确："To use the default value, set to null"。**没有 @null 就无法传 null**，属性下限/上限/步长全设不了默认值 | `@null` | ✅ 必须 |
| **`float?`/`int?` 可空** | 同上 | 要传可空数值（非 null 但可空类型） | `@float:`/`@int:` 解析后再考虑是否包装成 Nullable | 视实现 |
| **`Vector`（System.Windows.Vector）** | `CreateVector2DProperty(..., Vector? lowerBound, ...)` | 文档例：`new Vector(0,0)`，需 `using System.Windows` | `@vector:0,0` | ✅ 必须（Vector2D 属性） |
| **`Vector3D`** | `CreateVector3DProperty(..., Vector3D? ...)` | `using System.Windows.Media.Media3D` | `@vector3d:0,0,0` | ✅ 必须 |
| **`Quaternion`** | `CreateVector4DProperty(..., Quaternion? ...)` | `using System.Windows.Media.Media3D` | `@quaternion:x,y,z,w` | ✅ 必须 |
| **`Dictionary<string,int>`** | `CreateCustomEnumProperty(name, displayName, category, Dictionary<string,int> options)` | 已在原方案 `@dict:` | `@dict:{"a":1}` | ✅ 已有 |
| **`Type`** | `CreateProperty(Type dataType, ...)` / `IsAbstract(Type)` | 已在原方案 `@type:` | `@type:` | ✅ 已有 |
| **`AnimationTargetPropertyAttributeEnum`** | `CreateBinding(Property, AnimationTargetPropertyAttributeEnum, string)` | skill 已知 9=WHOLE_PROPERTY | `@enum:WHOLE_PROPERTY` | ✅ 已有 |
| **库查询类型** `ComponentType`/`BrushType`/`NodeComponentType`/`NodeEffectType2D`/`RenderPassType` | `CreateComponentNode(name, Node, ComponentType)` 等 | 来自 `xxxTypeLibrary.GetComponentType(name)`，桥内部 FindComponentType 已处理 | `@obj:` 或内部查找 | 视需要 |
| **`object` DefaultValue** | `Property.DefaultValue` 设置器 | 任意值，正好受益于 @int:/@string: 消歧义 | 复用各标识 | 可选 |

### 3.7 关键结论

1. **`@null` 是最重要的遗漏**。没有它，CreateFloatProperty/CreateIntProperty/CreateVectorXxxProperty
   的 lowerBound/upperBound/step 三个可空参数**根本没法传缺省值 null**（当前 `ResolveSingleArg`
   把 JSON null 会变 null，但字符串""或显式 null 语义不清）。必须加。
2. **Vector/Vector3D/Quaternion** 必须加。这些是创建 Vector2D/3D/4D 属性的核心参数类型。
   命名空间是 `System.Windows.Vector`、`System.Windows.Media.Media3D.Vector3D/Quaternion`（不是 Kanzi）。
3. **enum 仍需 @enum:**，但大部分 enum 参数（如 AnimationTargetPropertyAttributeEnum）
   当前靠 FindEnumMethod 自动转已可用，@enum: 是锦上添花的精准版。
4. 库查询类型（ComponentType 等）桥内部的 FindComponentType 已能处理，外部一般不用显式传。

### 3.8 补充后的完整标识总表（v4）

| 类型 | 标识 | 示例 | 来源 |
|------|------|------|------|
| int | `@int:` | `@int:123` | 基础 |
| long | `@long:` | `@long:...` | 基础 |
| float | `@float:` | `@float:1.5` | 基础 |
| double | `@double:` | `@double:3.14` | 基础 |
| decimal | `@decimal:` | `@decimal:99.9` | 基础 |
| bool | `@bool:` | `@bool:true` | 基础 |
| string | `@string:` | `@string:123` | 基础 |
| enum | `@enum:` | `@enum:WHOLE_PROPERTY` | 基础 |
| **null** | `@null` | `@null` | **API 新增** |
| Type | `@type:` | `@type:string` | 已有 |
| 对象 | `@obj:` | `@obj:10`（兼容 `@obj10`） | 已有 |
| 节点 | `@node:` | `@node:/Screens/...` | 基础 |
| 字典 | `@dict:` | `@dict:{"a":1}` | 基础 |
| **Vector2D** | `@vector:` | `@vector:0,0` | **API 新增** |
| **Vector3D** | `@vector3d:` | `@vector3d:0,0,0` | **API 新增** |
| **Quaternion** | `@quaternion:` | `@quaternion:0,0,0,1` | **API 新增** |

---

## 四、版本管理与实现步骤

### 4.1 版本策略（按老隋要求）

- ❌ 不改 `v3_merged`（保留为稳定版本）
- ✅ 新建 **`v4_parameter_tags`**，复制 v3_merged 全部文件后修改
- 只改 `KzMCPReflectionBridge.cs`（ResolveSingleArg + FindEnumMethod + 泛型分支）

### 4.2 实现步骤

1. 复制 v3_merged → v4_parameter_tags
2. 改 `ResolveSingleArg`：加显式标识分支（3.3）
3. 改 `FindEnumMethod`：支持 `@enum:` 精准匹配
4. 改泛型分支：`@obj:`/`@type:`/`@enum:` 作为 T 的解析
5. 结构验证（括号平衡）+ 发飞书
6. 老隋在 Windows 编译、重启测试

---

## 五、确认结果（老隋 16:30 拍板）✅

1. **`@null` 确认添加**（API 盘点最大发现）：给 CreateFloat/Int/Vector 属性的 lowerBound/upperBound/step 传缺省 null。
2. **Vector 系确认添加**：`@vector:`(2D)/`@vector3d:`/`@quaternion:`（System.Windows / Media3D）。
3. **`@long:`/`@double:`/`@decimal:`/`@char:`/`@byte:` 全都要**（避免后续有问题）。
4. **`@string:123` 强制字符串** 确认保留（解决"想传字符串'123'却被转int"歧义）。
5. **enum 用精准 `@enum:NAME`**（由 FindEnumMethod 按目标方法参数类型 Enum.Parse）。
6. **`@obj10` 老格式** 保留兼容；新格式 `@obj:10` 也支持。

> 已全部实现于 v4_parameter_tags，待老隋编译测试。

---

## 六、实施前提醒

- 这是较大重构，涉及 ResolveSingleArg + FindEnumMethod + 泛型分支 + CreateNode 可能联动
- 改完需重新编译 + 重启 Studio 才生效
- 本机 Linux 无法编译，只能做结构检查
- v3_merged 保持不动，新版本 v4 独立，失败可回退
