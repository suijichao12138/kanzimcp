# v5 线程策略版 — 说明（v5b 修复：离线直接跑当前线程池线程）

> 基于 **v4_parameter_tags** 新建的独立版本，**不动原 v4 插件**。
> 目的：把"状态机创建"类反射调用放到**非UI线程**执行以提速，其余反射调用**仍在 UI 线程**。
> v5b 关键修复：离线调用**弃用自建线程**，改为**直接在当前线程池线程上同步执行**——与旧版"不强制UI线程"行为一致，不再崩溃。

## v5b 改动（修复崩溃）

### 崩溃根因（v5 初版/单线程版）
- v5 初版用 `Task.Factory.StartNew(...).Result` 反复派线程池线程：不崩，但慢(6.9s/state) + Begin/Commit 报错（批次跨线程横跳破坏线程亲和）。
- v5 单线程版用**手工 `new Thread()` 原生线程** + BlockingCollection + TCS：**崩**，无log（原生层访问违例，绕开线程池托管上下文）。
- 验证：旧版"不强制UI线程"（HTTP版）在线程池线程上同步跑，**不崩**。

### v5b 正确做法（本次）
- **不在 Invoke 里创建任何新线程**。
- KzMCPServerClient 本身已在 `Task.Run(async ...)` 里调用 `ExecuteToolAsync` → 当前线程**已经是 .NET 线程池线程**。
- runOffUiThread=true 时，**直接在当前线程池线程上同步执行 `InvokeCore`**（跳过 Dispatcher.Invoke），与旧版 HTTP 行为一致 → 不崩。
- runOffUiThread=false 仍 `Dispatcher.Invoke` 切 UI 线程（维持 v4 行为）。
- 删除了 v5 单线程版的 `RunOffThread / OffThreadWorkerLoop / BlockingCollection / Thread` 全部死代码。

## 改动清单

### 1. KzMCPReflectionBridge.cs
- 新增 `Invoke(string target, string method, object[] args, bool runOffUiThread)` 重载：
  - `runOffUiThread = false`（默认）：维持 v4 原行为，`Dispatcher.Invoke` 切到 UI 线程执行。
  - `runOffUiThread = true`（v5b）：**直接在当前线程池线程上同步执行 `InvokeCore`**，避开 UI 线程 Dispatcher 开销。因为 KzMCPServerClient 已在 `Task.Run` 里调用到这里，当前线程本身就是线程池托管线程——与旧版"不强制UI线程"行为完全一致，不会崩。
- **绝不新建线程**：不在 Invoke 里创建任何 `Thread`/`StartNew`/`Dispatcher`（那两种都会绕开线程池托管上下文 → 原生层访问违例崩溃）。
- 原 3 参 `Invoke` 保留并委托到 4 参版本（`false`），保证其他调用方（聊天、窗口等）行为不变、向后兼容。
- 新增线程策略判定方法 `ShouldRunOffUiThread(target, method, args)`（实例方法）+ `IsStateTarget(target)`。

### 2. KzMCPServerClient.cs
- `tools/call` → `kz_invoke` 分派处改为：
  - `bool offUi = _bridge.ShouldRunOffUiThread(target, method, rawArgs);`
  - `_bridge.Invoke(target, method, rawArgs, offUi)`
- 日志里打印线程策略：`↳ 线程策略: 非UI线程 / UI线程`。

## 哪些调用走"非UI线程"
1. `BeginBatchModification` / `CommitBatchModification`
2. `CreateProjectItem` 且类型为 StateManager / StateGroup / State / StateObject（或其包装类型）
3. `set_TargetObjectPath` / `get_TargetObjectPath`
4. `set_Item` / `get_Item`，且 target 解析出的运行时对象是 State/StateObject 相关包装器

其余所有反射调用（普通节点属性、绑定、查询等）**仍走 UI 线程**，保持 v4 稳定行为。

## 风险提示（务必先小范围测试）
- Kanzi Studio 是 WPF 应用，大量对象（尤其 BindingHost/DSO）**只能在 UI(Dispatcher)线程访问**。
- **v5b 已修复崩溃点**：离线调用不再新建线程，而是直接在当前线程池线程同步跑（与旧版不强制UI一致），理论上不崩。
- **仍需小规模（2~5 状态）实测**：确认不崩、Begin/Commit 不报错、对象正确。
- 注意：多线程初版实测 Begin/Commit 报错（批次跨线程横跳）；v5b 因为每次请求都在同一条 Task.Run 线程池线程内串行处理整个批次，理论上避免了横跳，但仍需实测验证。
- 若遇"调用线程无法访问此对象"异常，说明该方法不能离线，应回退 v4（`runOffUiThread` 保持 false）。

## 构建
- Windows + .NET Framework 4.8 / Visual Studio 2022 编译（WPF 插件，Linux 上无法编译）。
- 用 `KzMCPChatPlugin.sln` 打开，Reference `PluginInterface` 指向 `..\v1_mcp_server\PluginInterface.dll`（如路径变化请调整）。
- 产出 `bin\Debug\KzMCPChatPlugin.dll` 部署到 Kanzi Studio 插件目录。

## 版本备注
- 与原 v4 相比，仅新增线程策略能力，未改动任何既有反射逻辑/参数标识解析。
- 若测试不理想，回退到 v4 即可，v4 源目录不受影响。
