# 动画创建 — 进度统计与能力边界

> 记录日期：2026-08-07
> 对应插件版本：v5_thread_policy（当前最新版，KzMCPReflectionBridge.cs / KzMCPServerClient.cs 8月6日更新）
> 测试对象：Kanzi Studio 实际工程（123.57.85.81 中继）

---

## 0. 一句话结论

插件是**"透传式"反射桥**：它不新建任何 Kanzi 类型的实例，只负责把**已认识并能拿到引用的对象/参数**传进 Kanzi 已有的方法。
因此——**只有落在「已支持的创建路径 + 已支持的参数形式」内的类型能构建；需要插件专门构造 Kanzi 类实例（关键帧、引用对象、集合等）的，目前都构建不了。**
这不是 bug，而是反射桥机制的固有边界。要打破，得给插件加专门的构建工厂。

---

## 1. 能创建的类型（✅ 已实测通过）

所有创建统一走 `CreateProjectItem` 泛型/非泛型反射，`runtimeType` 用类型名字符串驱动。

| 类型 | 创建方法（参数形式） | 实测结果 |
|------|---------------------|----------|
| **动画数据项（Animation）** | `CreateProjectItem("Animation", name, 动画库)` | ✅ 可建（如 __TEST_ANIM1__），落在 AnimationLibrary 下 |
| **动画组（AnimationClip）** | `CreateProjectItem("AnimationClip", name, 动画组库)` | ✅ 可建（如 __TEST_CLIP__），落在 Animation Clips 库 |
| **动画子片段（AnimationChildClip）** | `CreateProjectItem("AnimationChildClip", name, 动画组)` | ✅ 可建，落在动画组下 |
| **子片段指向动画** | `set_Item("AnimationChildClipTargetItem", "@anim:名称")` | ✅ 成功（关键突破） |

> ⚠️ 注意：`@anim:名称` 之所以能工作，是**插件对未知字符串原样透传**，Kanzi Studio 端把它内部解析成资源引用对象。
> 当前代码里没有对 `@anim:` 特判（grep 不到），属于"未知字符串兜底透传 → Studio 端解析"的巧合可用路径。

### 状态机链路（非动画，但同属"能创建"范畴，供对照）
StateManager / StateGroup / State / StateObject 均可建，`runOffUiThread=true` 已实现在离线线程池线程上批量跑（full 737 状态 11 分钟）。

---

## 2. 能作为参数解析的**参数形式**（插件 ResolveArgs 支持的标识）

- `@objN` / `@obj:N` — 对象引用（_objectStore 缓存）
- `@type:类型名` — .NET Type 对象（CreateProperty<T> 等泛型类型实参）
- `@vector:` / `@vector3d:` / `@quaternion:` — 数学类型
- `@enum:名称` — 交给 FindEnumMethod 按目标方法签名精准 Enum.Parse
- `@null` — 可空参数
- `@dict:{json}` — 字典
- `@node:路径` / `/路径`（含 / 的非 @ { 开头字符串自动尝试）— 节点对象
- `@project` / `@studio` / `@projectItem` — 特殊对象
- `@anim:名称` — 通过"未知字符串原样透传"间接可用（Studio 端解析，非插件显式支持）
- 裸 int / bool / float / string — 自动猜测兜底

**边界**：以上之外的**对象实例**（如需要构造的 `ProjectItemReference<T>` / `NodeReference<T>` 引用对象、集合对象、关键帧 TimelineEntry/TimelineSequence 实例）**构造不了、也传不进去**。

---

## 3. 不能创建的类型（❌ 已实测/确认失败）

| 类型/操作 | 现象 | 根因 |
|-----------|------|------|
| **关键帧（TimelineEntry / TimelineSequence）** | `CreateProjectItem` 建不了；动画项 children 为空，关键帧数据在 DynamicProperty 底层，public 接口摸不到 | 关键帧是引擎级数据结构，插件反射桥没有专门的构造/暴露接口 |
| **动画组引用动画（AnimationDataChildReferences）** | `clip.set_Item("AnimationDataChildReferences", <任何形式>)` 全失败：「cannot be converted to studio internal property value」。传 @obj / @anim: / 路径 / 名字 都不行 | 这是**引用图操作**（需要构造引用对象实例），非普通属性赋值；插件无专门处理 |
| **组内多个动画的关键帧时序编排** | 同上，卡在关键帧不可构造 | 依赖关键帧能力 |
| **现有 01circle_innerRadius 那类组** | 现有组引用方式是 AnimationDataChildReferences 直接指向动画项（无 childclip），AnimationClipStartTime=0/EndTime=2.5 | 而这正是插件设不了的那条路径 |

---

## 4. 关键认知（反编译 PluginInterface.dll 后确认的结构）

Kanzi 动画分两层：
- **Animation Data**：单个关键帧动画（`Internal.ctrlState`、`Internal.stateAnimationOpacity` 等，库内现有 24 个）
- **Animation Clips**：动画组容器，可引用多个 Animation Data 合成复杂动画（库内 18 组）

公开接口（反编译）：
- `Animation` / `AnimationProperties`：`AnimationTargetPropertyName` / `AnimationTargetPropertyAttribute` / `AnimationDataChildReferences`
- `AnimationClip` / `AnimationClipProperties`：`AnimationClipStartTime` / `EndTime` / `AutoSizeToContent` / `EditRootNode`
- `AnimationChildClip` / `AnimationChildClipProperties`：`AnimationChildClipTargetItem` ← 关键引用属性（已通过 @anim: 打通）
- `TimelineEntry` / `TimelineSequence` / `TimelineEntryTarget`：关键帧结构（StartTime / Duration / InputPropertyName / InputType / PlaybackMode）
- 枚举：`AnimationTargetPropertyAttributeEnum`（WHOLE_PROPERTY=9 等）、`TimelineEntryInputType`（TIME/PROPERTY）、`TimelineEntryPlaybackMode`（NORMAL/REVERSE/PING_PONG）

---

## 5. 现有调整思路（方案讨论）

### 2026-08-07 实测确认（连接中继复查两大卡点，工程无损：临时组 _ABTEST2_-_ABTEST4_ 全部 Delete() 清理，库内无残留）

**复测结论：**
1. **卡点1（AnimationDataChildReferences 引用图）已精确定位根因层级**：
   - 用 `@anim:` / `@obj:` / 裸名 / `#ClassId/路径` 四种形式 set 全部报 **「cannot be converted to studio internal property value」**——错误来自**插件端 set_Item 的字符串→属性值转换层**，不是 Studio 端。
   - 对比：`AnimationChildClipTargetItem` 同样用 `@anim:` 能 success——**差异在属性类型**，不在参数形式。AnimationDataChildReferences 是引用图属性，插件无专门转换；而 AnimationChildClipTargetItem 走的是字符串透传被 Studio 认。
2. **卡点2（关键帧）根因进一步明确**：
   - TimelineEntry 系属性（TimelineEntryInputPropertyName/TargetReference/Duration/StartTime/RepeatCount）在 **Animation wrapper 上 `get_Item 报 Property not found`**——这些是**关键帧条目（TimelineEntry 子对象）的属性**，不是动画项自身的属性。关键帧不是 CreateProjectItem 能造的独立工程项。

---

## 5b. 方案A（引用构造）当前可行性判断（更新）

- `AnimationChildClip` 绕通**确认真生效（非空调用）**：临时组→建 childclip→挂 `@anim:InnerRadius_1`，读回 `child.TargetItem` 得到 `GenericProjectItemReference\`1` 引用对象（指向 @obj296）。两层结构成立，清理干净。
- 但 `AnimationDataChildReferences` 仍打不通——因为它需要的是**直接挂动画项集合**而非 childclip 引用。childclip 绕通只能覆盖「组下有子片段挂动画」的一级结构。
- 反射构造 `ResourceReference<T>` 可行性：反编译确认 `ResourceReference\`1`/`NodeReference\`1`/`GenericProjectItemReference\`1` 均有 2 个 `.ctor`，CLR 层可构造；但 `T:NodeResource` 泛型约束能否反射绕过、构造实例能否过插件 set_Item 转换仍未实测（这是路线A的下一验证点）。

---

## 6. 现有调整思路（方案讨论）

### 方向 A：插件加"引用构造工厂"（治本，推荐）
在 KzMCPReflectionBridge 增加对 `ProjectItemReference<T>` / `ResourceReference<T>` / `NodeReference<T>` 的**纯反射构造能力**：
- 用 `Activator.CreateInstance` + 泛型闭合 + 构造参数注入，构造出引用对象实例
- 这样 `AnimationDataChildReferences` 这类引用图属性就能传入真实引用对象
- 完全符合"全反射、不引用具体 Kanzi 类型"的核心约束
- **风险**：`ResourceReference<T>` 有 `T:NodeResource` 泛型约束，需确认动画项/动画组是否满足约束类型；能否在运行时用反射绕过构造仍待验

### 方向 B：给关键帧加专用构建接口（治本）
关键帧 TimelineEntry/TimelineSequence 无法用 CreateProjectItem 建，需插件针对 wrapper 底层/DynamicProperty 写专门创建逻辑，或在引擎 API 层操作。
- 反编译确认这些类存在公开接口，但非 CreateProjectItem 能造
- 工作量大，需专门探测每个字段的 set 路径

### 方向 C：走 AnimationChildClip 绕通部分引用（治本不彻底，已通）
已打通：子片段 `AnimationChildClipTargetItem` → 动画项。
- 但 **AnimationDataChildReferences 不联动**，Studio 是否完全认这套结构仍待确认
- 只能算是"能建出节点了"，时序/引用完整性存疑

### 方向 D：走现有动画（治标，零开发）
用库里**已有的 24 个动画项 + 18 个动画组**直接组装，不改插件。
- 若老隋实际需求只是"把这些现有动画用起来"，选择 D 即可上线
- 若要"从零建任意动画/关键帧"，必须 A/B

---

## 6. 需求结论（老隋 2026-08-07 确认）

**两点都需要**：
- ✅ **获取/使用现有动画**（读现有 24 动画项 + 18 动画组的引用、字段、组装）
- ✅ **从零创建**动画 + 关键帧（建新 Animation / AnimationClip / 关键帧数据）

→ 即 **方案 D（用现有）+ 方案 A/B（从零建）两条腿都要**，不是二选一。

## 7. 下一步（两条腿并行）

### 路线一：用现有动画（方案 D，零插件开发）
- [ ] 读现有动画项/动画组：`get_Item` 各字段（AnimationTargetPropertyName/Attribute、AnimationDataChildReferences、AnimationClipStartTime/EndTime）
- [ ] 确认现有组的引用方式与字段结构，作为从零创建的模板
- [ ] 确认能否把现有动画项/组拿去直接赋给目标节点属性

### 路线二：从零创建（方案 A/B，需插件增强）
- [ ] **A-引用构造工厂**：反射构造 `ProjectItemReference<T>`/`ResourceReference<T>`/`NodeReference<T>`，打通 `AnimationDataChildReferences` 引用图——先小规模验证泛型约束（T:NodeResource）能否反射绕过
- [ ] **B-关键帧接口**：探测 TimelineEntry/TimelineSequence 各字段（StartTime/Duration/InputPropertyName/InputType/PlaybackMode）的 set 路径，评估能否在 wrapper/DynamicProperty 底层创建
- [ ] 打通后：验证 从零建「动画项→动画组→动画组引用动画→关键帧时序」全链路

### 通用
- [ ] 工程状态：动画库 24 项、Animation Clips 18 组，测试残留已清理干净
- [ ] 每一步在 Studio 实测，不以文档为准（遵守 MEMORY.md 操作前规则）
