<UserControl x:Class="KzMCPChatPlugin.KzMCPServerWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 地址栏 + 连接 -->
        <Grid Grid.Row="0" Margin="0,0,0,6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="Relay：" VerticalAlignment="Center"
                       FontSize="12" Foreground="#888" Margin="0,0,4,0"/>
            <TextBox Grid.Column="1" x:Name="RelayUrlBox"
                     Text="ws://10.10.118.152:58080/username"
                     FontSize="11" VerticalAlignment="Center"/>
            <Button Grid.Column="2" x:Name="ConnectMCPBtn" Content="连接" 
                    Width="48" Height="24" FontSize="11" Margin="4,0,2,0"
                    Click="ConnectMCP_Click"/>
            <Button Grid.Column="3" x:Name="DisconnectMCPBtn" Content="断开" 
                    Width="48" Height="24" FontSize="11"
                    IsEnabled="False" Click="DisconnectMCP_Click"/>
        </Grid>

        <!-- MCP Server 状态 -->
        <Border Grid.Row="1" Background="#1A1A1A" BorderBrush="#444" BorderThickness="1" 
                Margin="0,0,0,6" Padding="6">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" x:Name="StatusIcon" Text="⚫" FontSize="14" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" x:Name="StatusText" Text="未连接" 
                           FontSize="12" Foreground="#AAA" VerticalAlignment="Center" Margin="6,0,0,0"/>
                <TextBlock Grid.Column="2" x:Name="ChannelText" Text="" 
                           FontSize="11" Foreground="#888" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- 工程信息 -->
        <Border Grid.Row="2" Background="#1A1A1A" BorderBrush="#444" BorderThickness="1" 
                Margin="0,0,0,6" Padding="6" Height="24">
            <TextBlock x:Name="ProjectInfo" Text="工程：无" FontSize="11" Foreground="#888"/>
        </Border>

        <!-- 日志 -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Grid.Row="0" Text="日志" FontSize="11" Foreground="#888" Margin="0,0,0,2"/>
            <TextBox Grid.Row="1" x:Name="LogBox" 
                     IsReadOnly="True"
                     Background="#1E1E1E"
                     Foreground="#AAA"
                     FontSize="11"
                     BorderBrush="#555" BorderThickness="1"
                     Padding="4"
                     VerticalScrollBarVisibility="Auto"
                     TextWrapping="Wrap"/>
        </Grid>
    </Grid>
</UserControl>
